export type Routes = {
    name: string,
    path: string
}

export type Tool = {
    id: number
    name: string
    link: string
    color: string
}

export type ContactInfo = {
    type: string
    name: string
    link: string
}